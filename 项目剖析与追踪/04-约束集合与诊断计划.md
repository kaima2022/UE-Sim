# 约束集合与诊断计划

## 生成日期
2025-01-25

## 项目状态
- **完成度**: 85%
- **核心阻塞**: T002接收机制失效

## 硬性约束

### 协议层约束
| 约束项 | 值 |
|--------|-----|
| PDC ID范围 | IPDC=[1,512], TPDC=[513,1024] |
| MTU | 1500B(default), 9000B(jumbo) |
| TPDC重传 | maxRetry=5, RTO=200ms~2s |
| TPDC窗口 | 128 |
| IPDC窗口 | 64 |
| 交付模式 | RUD/ROD/UUD/UOD |

### 网络层约束
| 约束项 | 值 |
|--------|-----|
| 最大设备数/信道 | 1000 |
| FEP提取 | MAC地址[4:5]字节 |
| 传播延迟 | 100ns |

### 成功判据
- 吞吐效率 >= 50%
- 丢包率 < 1%
- 端到端路径完整

## 已识别问题

| ID | 严重性 | 位置 | 症状 |
|----|--------|------|------|
| ISS-001 | CRITICAL | soft-ue-channel.cc:206 | FEP路由匹配失败 |
| ISS-002 | CRITICAL | soft-ue-net-device.cc:454-458 | 接收过滤过严 |
| ISS-003 | MEDIUM | soft-ue-net-device.cc:586-589 | 回调参数错误 |
| ISS-004 | MEDIUM | soft-ue-net-device.cc:560-569 | PDC回调未注册 |
| ISS-005 | LOW | src/soft-ue/test/ | 测试覆盖不足 |

## 诊断计划

### D1: 日志追踪
```bash
NS_LOG="SoftUeChannel=debug:SoftUeNetDevice=debug" ./ns3 run Soft-UE 2>&1 | head -200
```

### D2: FEP匹配逻辑检查
- 文件: `src/soft-ue/model/network/soft-ue-channel.cc`
- 行号: 206
- 关注: `GetDestinationFepForDevice()` 返回值

### D3: 接收过滤检查
- 文件: `src/soft-ue/model/network/soft-ue-net-device.cc`
- 行号: 454-458
- 关注: `destFep != m_localFep` 条件

### D4: 初始化顺序验证
确认调用链: `SetChannel → Initialize → SetNode`

## 关键代码路径

### 发送路径
```
Application.SendPacket()
  → SesManager.ProcessSendRequest()
    → PdsManager.ProcessSesRequest()
      → PdcBase.SendPacket()
        → SoftUeNetDevice.Send()
          → SoftUeChannel.Transmit()
```

### 接收路径 (待验证)
```
SoftUeChannel.ScheduleReceive()
  → SoftUeNetDevice.ReceivePacket()
    → ProcessReceiveQueue()
      → m_receiveCallback()
        → PdsManager.ProcessReceivedPacket()
          → SesManager.ProcessReceiveRequest()
```

## 修复优先级

1. **P0**: ISS-001 + ISS-002 (接收机制核心问题)
2. **P1**: ISS-003 + ISS-004 (回调参数与注册)
3. **P2**: ISS-005 (测试覆盖完善)
4. **P3**: 200Gbps性能压测验证
